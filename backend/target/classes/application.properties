# Server Configuration
server.port=8080
server.servlet.context-path=/api

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:kids_shopping_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# OAuth2 Configuration
# Set these environment variables or replace with your actual values
spring.security.oauth2.client.registration.google.client-id=384695007720-4tim1el0uq77rlb90t649lf756ennotn.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# Redirect URI - must be the OAuth2 callback endpoint (where Google sends users back)
# Final callback URL: http://localhost:8080/api/login/oauth2/code/google
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/api/oauth2/authorization/google

# JWT Configuration
jwt.secret=${JWT_SECRET:your-super-secret-jwt-key-for-hugscape-kids-shopping-website-2025}
jwt.expiration=86400000

# Logging
logging.level.com.hugscape=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
